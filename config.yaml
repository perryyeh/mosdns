log:
  level: info
  file: "/dev/shm/mosdns.log"

api:
  http: "0.0.0.0:9091"

include:
  - "./data.yaml"
  - "./dns.yaml"

plugins:
  # 转发组 - 直连
  - tag: forward_direct_group
    type: fallback
    args:
      primary: ali
      secondary: tencent
      threshold: 50
      always_standby: true

  # 转发组 - 代理
  - tag: forward_proxy_group
    type: fallback
    args:
      primary: cloudflare
      secondary: google
      threshold: 100
      always_standby: true

  # fakeip 分发：AAAA → v6, A → v4
  - tag: sequence_fakeip
    type: sequence
    args:
      - matches: "qtype 28"
        exec: $forward_fakeipv6
      - matches: "qtype 1"
        exec: $forward_fakeipv4

  # proxy 域名通用处理：拦截 64/65，A/AAAA 走 fakeip，其他走代理直查
  - tag: sequence_proxy_fakeip
    type: sequence
    args:
      - matches: "qtype 64 65"
        exec: reject 0
      - matches: "qtype 64 65"
        exec: return
      - exec: $sequence_fakeip
      - matches: has_resp
        exec: return
      - exec: $forward_proxy_group

  # 不在名单中的域名
  - tag: sequence_not_in_list
    type: sequence
    args:
      # SVCB/HTTPS (qtype 64/65) 强制 NODATA，防止 ipv6hint/ipv4hint 泄露真实 IP
      # 必须在 forward 之前拦截，否则 remote 返回的 SVCB 会被透传
      - matches: "qtype 64 65"
        exec: reject 0
      - matches: "qtype 64 65"
        exec: return

      # 先带 ECS 查 Cloudflare/Google，用返回 IP 判定走直连还是 fakeip
      - exec: $ecs_cn
      - exec: $forward_proxy_group

      # A/AAAA 无 IP → 清 ECS 后用国内 DNS 补查；仍无结果则返回空结果
      - matches:
        - "qtype 1 28"
        - "!resp_ip 0.0.0.0/0 ::/0"
        exec: $no_ecs
      - matches:
        - "qtype 1 28"
        - "!resp_ip 0.0.0.0/0 ::/0"
        exec: $forward_direct_group
      - matches:
        - "qtype 1 28"
        - "!resp_ip 0.0.0.0/0 ::/0"
        exec: return

      # A/AAAA 是 CN IP → 清 ECS 后用国内 DNS 重查，拿更合适的国内 IP
      - matches:
        - "qtype 1 28"
        - "resp_ip $geoip_cn"
        exec: $no_ecs
      - matches:
        - "qtype 1 28"
        - "resp_ip $geoip_cn"
        exec: $forward_direct_group
      - matches:
        - "qtype 1 28"
        - "!resp_ip 0.0.0.0/0 ::/0"
        exec: return

      # 国内 DNS 返回 CN IP → 记录并返回真实 IP；若返回海外 IP，继续走 fakeip
      - matches: "resp_ip $geoip_cn"
        exec: query_summary not_in_list_direct
      - matches: "resp_ip $geoip_cn"
        exec: return

      # 非 A/AAAA：直接返回 Cloudflare/Google 真实结果（64/65 已在前面拦截）
      - matches: "!qtype 1 28"
        exec: return

      # 剩下的 A/AAAA（海外 IP）→ fakeip
      - exec: drop_resp
      - exec: $sequence_fakeip
      - matches: "resp_ip $fakeip_cidr"
        exec: query_summary not_in_list_fake
      - exec: return

  # 主逻辑
  - tag: sequence_main
    type: sequence
    args:
      # 1. geosite_local 交给 local 去解析
      - matches: qname $geosite_local
        exec: $local
      - matches: qname $geosite_local
        exec: accept

      # 2. geosite_my_hosts 直接读取里面的解析地址
      - exec: $geosite_my_hosts
      - matches: has_resp
        exec: accept

      # 3. 自定义黑名单：最高优先级，压过所有公共规则和自定义白/灰名单
      - matches: qname $geosite_my_blacklist
        exec: reject 3
      - matches: qname $geosite_my_blacklist
        exec: return

      # 4. 自定义灰名单：64/65 拦截，A/AAAA 走 fakeip，其他 qtype 走代理真实查询；命中后不降级到后续规则
      - matches: qname $geosite_my_greylist
        exec: $sequence_proxy_fakeip
      - matches: has_resp
        exec: accept
      - matches: qname $geosite_my_greylist
        exec: return

      # 5. 自定义白名单：走 ali 和 tencent，压过公共 proxy
      - matches: qname $geosite_my_whitelist
        exec: $forward_direct_group
      - matches: has_resp
        exec: accept

      # 6. 公共 blocklist：直接返回无法解析
      - matches: qname $geosite_block
        exec: reject 3
      - matches: qname $geosite_block
        exec: return

      # 7. 公共 proxy：64/65 拦截，A/AAAA 走 fakeip，其他 qtype 走代理真实查询；优先于公共 direct
      - matches: qname $geosite_proxy
        exec: $sequence_proxy_fakeip
      - matches: has_resp
        exec: accept
      - matches: qname $geosite_proxy
        exec: return

      # 8. 公共 direct：走 ali 和 tencent 去解析
      - matches: qname $geosite_direct
        exec: $forward_direct_group
      - matches: has_resp
        exec: accept

      # 9. 剩下的不在名单中的域名 兜底逻辑
      - exec: $sequence_not_in_list
      - matches: has_resp
        exec: accept

  # 启动服务器
  - tag: udp_main
    type: udp_server
    args:
      entry: sequence_main
      listen: ":53"

  - tag: tcp_main
    type: tcp_server
    args:
      entry: sequence_main
      listen: ":53"

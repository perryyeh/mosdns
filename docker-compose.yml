services:
  mosdns:
    image: irinesistiana/mosdns:latest
    container_name: mosdns
    hostname: mosdns
    restart: always
    mac_address: ${mosdnsmac}
    networks:
      macvlan:
        ipv4_address: ${mosdns4}
    volumes:
      - ./:/etc/mosdns/

networks:
  macvlan:
    external: true
    name: ${MACVLAN_NET}